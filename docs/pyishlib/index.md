# Python Library Reference (`pyishlib`)

> **Note:** This file is auto-generated by `scripts/build_pydocs.py`. Do not edit directly.

## [`command_runner`](command_runner.md)

Helper commands for running commands and common shell tasks

- `CommandRunner` -- Helper class for running commands and common shell tasks

## [`diff`](diff.md)

Diff utilities for comparing files.

Functions: `print_diff`, `print_new_file`, `print_binary_diff`

## [`dotfile`](dotfile.md)

Single dotfile representation and path-translation helpers.

- `ChangeType` -- Type of change to apply.
- `DotFile` -- Represents a single dotfile managed by the applier.

Functions: `translate_name`, `translate_path`, `reverse_translate_name`, `reverse_translate_path`

## [`dotfile_applier`](dotfile_applier.md)

Dotfile applier for managing dotfile repositories.

- `DotfileApplier` -- Three-stage dotfile applier.

Functions: `register_cli`

## [`dotfile_context`](dotfile_context.md)

Dotfile context for preprocessing variable tracking and expression evaluation.

- `DotfileContext` -- Context object for dotfile preprocessing.

## [`dotfile_finder`](dotfile_finder.md)

Dotfile path resolution and lookup.

- `DotfileFinder` -- Resolve arbitrary file paths to `DotFile` objects.

## [`dotfile_ignore`](dotfile_ignore.md)

Ignore-list handling for dotfile management.

- `DotfileIgnore` -- Encapsulates the complete set of ignore rules for dotfile discovery.

Functions: `load_ignore_file`

## [`dotfile_preprocessor`](dotfile_preprocessor.md)

Dotfile preprocessing: directive handling, variable substitution, metadata stripping.

- `DotFilePreprocessor` -- Preprocessor specialised for `DotFile` objects.

## [`dotfile_script`](dotfile_script.md)

Executable script with `@ish` directive preprocessing.

- `DotfileScript` -- A script file that is preprocessed and executed.

## [`file_preprocessor`](file_preprocessor.md)

Base file preprocessing: directive handling, variable substitution, metadata stripping.

- `FilePreprocessor` -- Base preprocessor for files with `@ish` directives.

## [`installer`](installer.md)

Helper library for package installing tasks

- `Installer` -- Installer class for installing packages.

## [`installer_apt`](installer_apt.md)

Helper library for apt package installing tasks

- `InstallerApt` -- Helper class for managing apt packages

## [`installer_brew`](installer_brew.md)

Helper library for package installing tasks

- `InstallerBrew` -- Helper class for managing packages via Homebrew

## [`installer_cargo`](installer_cargo.md)

Helper library for package installing tasks

- `InstallerCargo` -- Helper class for managing rust and cargo packages

## [`installer_config`](installer_config.md)

Classes to manage installer configuration

- `InstallerConfig` -- Class for handling installer configuration
- `InstallerConfigJSON` -- Class for handling installer configuration from a JSON file
- `InstallerConfigTOML` -- Class for handling installer configuration from a TOML file

## [`installer_custom`](installer_custom.md)

Custom script-based package installer.

- `InstallerCustom` -- Installer backend that runs user-provided install scripts.

## [`installer_pip`](installer_pip.md)

Helper library for package installing tasks

- `InstallerPip` -- Helper class for managing python packages via pip

## [`installer_winget`](installer_winget.md)

Helper library for package installing tasks

- `InstallerWinget` -- Helper class for managing packages via winget (Windows Package Manager)

## [`ish_comp`](ish_comp.md)

Common utilities for pyishlib: logging setup, prompts, and enums.

- `Choice` -- Enum for choices
- `IshLogFormatter` -- Custom formatter to prefix log levels

Functions: `setup_logging`, `prompt_yes_no_always`, `die`

## [`ish_config`](ish_config.md)

Shared configuration for pyishlib components.

- `IshConfig` -- Shared configuration for all pyishlib components.

## [`ish_metadata`](ish_metadata.md)

Read embedded __ISH__ metadata from files.

Functions: `merge_metadata`, `remove_metadata_blocks`, `read_metadata`, `scan_directory`, `register_cli`

## [`ishfiles`](ishfiles.md)

ishfiles -- dotfile manager built on pyishlib.
