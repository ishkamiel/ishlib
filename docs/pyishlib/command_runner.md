# `command_runner`

> **Note:** This file is auto-generated by `scripts/build_pydocs.py`. Do not edit directly.

Helper commands for running commands and common shell tasks

### `CommandRunner`

Helper class for running commands and common shell tasks

#### `__init__(cfg: Optional[IshConfig] = None, always_sudo: bool = False)`

#### `on_windows`

True if running on Windows

#### `dry_run`

Is dry-run mode enabled

#### `verbose`

Is verbose mode enabled

#### `quiet`

Is quiet mode enabled

#### `always_sudo`

Is always-sudo mode enabled, i.e., sudo without asking

#### `run_sudo(command: Iterable[str], force_sudo: Optional[bool] = False, kwargs = {})`

Run command with sudo (not available on Windows)

#### `run(command: Iterable[str], work_dir: Optional[Path] = None, quiet: bool = False, kwargs = {})`

Run command

#### `git(command: Iterable[str], work_dir: Optional[Path] = None, kwargs = {})`

Run git command using Commandrunner.run

#### `chdir(path: Path, mkdir: Optional[bool] = False, may_fail: Optional[bool] = False)`

Change directory to path, optionally creating it if it does not exist

#### `rm(path: Path, recursive: Optional[bool] = False)`

Remove path, optionally recursively

#### `mkdir(path: Path, parents: Optional[bool] = False)`

Create path, optionally creating parent directories

#### `copy(src: Path, dst: Path)`

Copy a file from src to dst, creating parent directories as needed

#### `on_ubuntu()`

Check if running on Ubuntu

#### `on_ubuntu_desktop()`

Check if running on Ubuntu Desktop

#### `which(command: str)`

Find the path to a command
